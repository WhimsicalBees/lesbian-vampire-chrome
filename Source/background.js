// chrome.browserAction.onClicked.addListener(
// 	function(tab) {
// 		localStorage["VampireLesbianEnabled"] = !isLesbianEnabled(localStorage["VampireLesbianEnabled"])
// 	}
// );

// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
//     if (request.method == "isEnabled")
//       sendResponse({status: localStorage['VampireLesbianEnabled']});
//     else
//       sendResponse({}); // snub them.
// });

// function isLesbianEnabled(val) {
// 	return val == "true"
// }
